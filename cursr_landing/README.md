# Causr landing page

Marketing site for Causr (Vite + React).

```bash
npm install
npm run dev
```

Primary CTA links open the developer dashboard at `http://localhost:5173` (`causr-dashboard`).
Instrumentation guide: [`../docs/INSTRUMENTATION.md`](../docs/INSTRUMENTATION.md).
